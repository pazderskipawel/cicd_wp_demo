name: Send Discord Notification

on:
  workflow_call:
    secrets:
      DISCORD_WEBHOOK_URL:
        required: true
jobs:
  notify-on-fail:
    runs-on: [self-hosted]
    steps:
      - name: Notify Discord on failure
        #https://github.com/marketplace/actions/actions-status-discord
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK_URL }}
          title: "❌ Build Failed or Cancelled"
          description: "Build in workflow `${{ github.workflow }}` failed or was cancelled on branch `${{ github.ref_name }}`."
          color: 0xff0000