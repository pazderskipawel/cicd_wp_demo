name: Main Pipeline

on:
  push:
    paths-ignore:
      - 'README.md'
      - 'LICENSE.md'
      - '.github/ISSUE_TEMPLATE/*'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true 

jobs:
  clean-up-last-build:
    uses: ./.github/workflows/environment_docker_cleanup.yml
    
  prepare-environment:
    uses: ./.github/workflows/environment_prepare.yml
    secrets: 
      inherit

  start-app:
    needs: [clean-up-last-build, prepare-environment]
    uses:  ./.github/workflows/app_run.yml
    secrets:
      inherit
  
  configure-app:
    needs: start-app
    uses: ./.github/workflows/app_configure.yml
    secrets:
      inherit

  validate-app:
    needs: configure-app
    uses: ./.github/workflows/app_validate.yml
    secrets:
      inherit

  notify-if-any-step-fails:
    needs: [prepare-environment, validate-app, start-app, configure-app]
    if: failure() || cancelled()
    uses: ./.github/workflows/notification_discord.yml
    secrets:
      inherit

  #TODO create artifact with app #58
 
  #stop-app:
  #  if: github.ref_name != 'main'
  #  needs: run-app
  #  uses: ./.github/workflows/environment_docker_cleanup.yml